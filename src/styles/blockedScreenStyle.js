import {StyleSheet} from 'react-native';

const BlockedScreenStyle = StyleSheet.create({
  slideContainer: {
    height: 87,
    borderRadius: 9,
    padding: 10,
    marginBottom: 10,
    width: '100%',
    backgroundColor: '#606060',
    alignItems: 'center',
    justifyContent: 'center',
  },
  alternateText: {
    fontSize: 13,
    fontWeight: 'bold',
    color: '#fff',
  },
});
export default BlockedScreenStyle;